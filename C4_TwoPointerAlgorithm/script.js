// Filter Algorithm
let num = [15, 20, 14, 9, 27, 25, 24, 47, 16, 85, 26];
let res = num.filter((num) => num >= 20 && num <= 30);
console.log(res);

// GuestsFinder.js
let Guests = ['Ana', 'Carlos', 'Cecilia', 'Daniel', 'Diana', 'Eduardo'];

function findPair(arr) {
  let start = 0;
  let next = 1;

  while (next < arr.length) {}
  return null;
}

console.log(findPair(Guests));
